# Core dependencies
# GPT-5.6 on Copilot is Responses API-only; 1.95 includes its streaming bridge.
litellm[proxy]==1.95.0
# LiteLLM 1.95 imports get_flat_dependant, removed by FastAPI 0.141.
fastapi==0.136.3
psutil>=5.9.0
requests>=2.31.0
dnspython>=2.4.0
tiktoken>=0.5.0

# Optional for monitoring
watchdog>=3.0.0